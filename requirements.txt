# Runtime dependencies, pinned to the versions this project was built and
# tested against. `pip install -r requirements.txt` reproduces the environment
# the test suite is green on.

langgraph==1.2.11
langgraph-checkpoint==4.2.0
langchain-core==1.6.2

litellm==1.100.1          # provider-agnostic LLM access
pydantic==2.13.5
pydantic-settings==2.15.0

fastapi==0.141.1
uvicorn[standard]==0.52.4

rank-bm25==0.2.2          # retrieval; no vector DB, no embedding API
httpx==0.28.1             # myScheme ingestion
beautifulsoup4==4.15.0
python-dotenv==1.2.3
